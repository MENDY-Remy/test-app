import 'package:flutter/material.dart';

class AproposSection extends StatelessWidget {
  const AproposSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text("Apropossss Sectionnnn")],
    );
  }
}
