import 'package:flutter/material.dart';

class CaptureSection extends StatelessWidget {
  const CaptureSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text("Capture Sectionnnn")],
    );
  }
}
